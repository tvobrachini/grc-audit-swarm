# grc-audit-swarm

## Agents

This project is managed by Genie CLI.

## Conventions

- Follow existing code style and patterns
- Write tests for new functionality
- Use conventional commits

## Agent skills

### Issue tracker

Issues live in GitHub Issues (`tvobrachini/grc-audit-swarm`) via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default canonical strings (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout: `CONTEXT.md` at repo root + `docs/adr/`. See `docs/agents/domain.md`.
