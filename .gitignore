# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# ─── SECRETS — NEVER COMMIT THESE ────────────────────────────────────────────
.env
.env.local
.env.*.local
*.secret
*_secret*
secrets.json
secrets.toml
.streamlit/secrets.toml

# Virtual environments
.venv/
venv/
env/
ENV/
env.bak/
venv.bak/

# ─── Persistent runtime data (user-specific, not for version control) ─────────
data/audit_checkpoints.sqlite
data/audit_sessions.json
data/scf_parsed.json
data/scf_data.xlsx
data/scf_data.csv

# Lab / test data with potentially sensitive content
lab_data/

# IDE & OS
.idea/
.vscode/
*.DS_Store
*.swp

# Streamlit cache
.streamlit/
evidence_vault/
ci_logs.txt

# Test/build caches
.coverage
frontend/.vite/

# Operational tmux logs — never commit
tmux-*.log

# Local tooling config — not for version control
.claude/
.genie/
